{
  "name": "@shamu/flows-plan-execute-review",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Canonical plan -> execute -> review flow for Shamu. Codex planner (GPT-5.4) -> Claude Opus 4.7 executor -> Codex reviewer (GPT-5.4) -> loop until approve or max iterations. Wires @shamu/core-flow runners to the production vendor adapters.",
  "exports": {
    ".": "./src/index.ts",
    "./flow": "./src/flow.ts",
    "./runners": "./src/runners.ts",
    "./schemas": "./src/schemas.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "clean": "rm -rf dist coverage .turbo"
  },
  "dependencies": {
    "@shamu/adapter-claude": "workspace:*",
    "@shamu/adapter-codex": "workspace:*",
    "@shamu/adapters-base": "workspace:*",
    "@shamu/core-flow": "workspace:*",
    "@shamu/shared": "workspace:*",
    "zod": "4.1.13"
  }
}
