{
  "name": "@shamu/ci",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Quality-gate wrapper around @redwoodjs/agent-ci: spawn-under-Bun driver, run-dir discovery, run-state + step-log parsers (TAP-13 + ESLint-stylish + tail fallback), ANSI strip, deterministic reviewer excerpt, CIRed/PatchReady domain-event projection.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "clean": "rm -rf dist coverage .turbo"
  },
  "dependencies": {
    "@shamu/shared": "workspace:*"
  },
  "devDependencies": {
    "@redwoodjs/agent-ci": "0.10.7",
    "@types/bun": "1.3.12",
    "typescript": "6.0.3",
    "vitest": "4.1.4"
  }
}
