{
  "name": "@shamu/linear-webhook",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Linear webhook receiver: Bun HTTP server with HMAC-SHA256 signature verification, timestamp window, nonce-cache replay protection, and a typed event union for issue-label-added / comment-created / status-changed. Plus a cloudflared tunnel wrapper scoped to /webhooks/linear.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "clean": "rm -rf dist coverage .turbo"
  },
  "dependencies": {
    "@shamu/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "1.3.12",
    "typescript": "6.0.3",
    "vitest": "4.1.4"
  }
}
